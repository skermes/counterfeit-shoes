/** @jsx React.DOM */

var Logo = React.createClass({
  render: function() {
    return (
      <img className="logo" src="logo.png" alt="SolAnvil" />
    );
  }
});

module.exports = Logo;
